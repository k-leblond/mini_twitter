<h1>Discover the community!</h1>

<div class="container">
  <ul>
    <% @posts.each do |post| %>
    <li>
    <%# <%= link_to "#{post.user.name}", user_path(post.user) %>
    <%= link_to "#{post.user.name}", user_path(post.user) %>
    <%= post.content %>
    </li>
    <% end %>
  </ul>
</div>
